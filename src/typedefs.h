#ifndef TYPEDEFS_H
#define TYPEDEFS_H

// Note: We use SDL's definitions for integer sizes, eg Uint32, Uint8 etc.
//       I would prefer using u32, u8, etc.

typedef float f32;
typedef double f64;

#endif // TYPEDEFS_H
